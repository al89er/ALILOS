name: Deploy ALILOS Webapp to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "webapp/**"
      - ".github/workflows/deploy-webapp.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Add GitHub Pages config
        run: |
          cat > webapp/config.js <<'EOF'
          window.ALILOS_CONFIG = {
            supabaseUrl: "${{ vars.ALILOS_SUPABASE_URL }}",
            supabaseAnonKey: "${{ vars.ALILOS_SUPABASE_ANON_KEY }}",
            deviceId: "${{ vars.ALILOS_DEVICE_ID }}"
          };
          EOF

      - name: Upload webapp artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: webapp

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
